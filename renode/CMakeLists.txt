if(HYDRV_MCU_FAMILY STREQUAL "F1")
    set_property(GLOBAL PROPERTY HYDRV_RENODE_BOARD_FILE "${CMAKE_CURRENT_SOURCE_DIR}/f1/board/stm32f1_board.repl")
elseif(HYDRV_MCU_FAMILY STREQUAL "F4")
    set_property(GLOBAL PROPERTY HYDRV_RENODE_BOARD_FILE "${CMAKE_CURRENT_SOURCE_DIR}/f4/board/stm32f4_discovery.repl")
else()
    message(WARNING "RENODE resources are not supported for HYDRV_MCU_FAMILY: ${HYDRV_MCU_FAMILY}")
endif()

set_property(GLOBAL PROPERTY HYDRV_RENODE_TEST_HELPER "${CMAKE_CURRENT_SOURCE_DIR}/HydrvRenodeTests.cmake")

set_property(GLOBAL PROPERTY HYDRV_PYHTONPATH "${CMAKE_CURRENT_SOURCE_DIR}")
